# وجين — خطة الفحص الشاملة للفرونت إند (Design System QA)

> **الهدف**: نوصل الموقع لحالة "جاهز للعرض على العميل" بدون مفاجآت: صفر مشاكل مسافات/تصميم، RTL مطابق لـ LTR، كل الشاشات مضبوطة، صفر كود ميت أو مكرر.
> **الطريقة**: كل بند له *معيار قبول* واضح (Pass/Fail) — مش "شكله حلو"، لأ: قيمة محددة من نظام التصميم.

---

## 0) نتيجة الفحص الآلي — الحالة الحالية

| الفحص | الأمر | النتيجة |
|---|---|---|
| بناء الإنتاج | `npm run build` | ✅ يمر (24 صفحة ثابتة) |
| فحص الأنواع | `npx tsc --noEmit` | ✅ صفر أخطاء |
| ESLint | `npx eslint .` | ❌ **2 errors + 2 warnings** |
| تطابق ملفات الترجمة | سكربت مقارنة المفاتيح | ✅ 228 = 228، صفر مفتاح ناقص |
| مكوّنات غير مستوردة | بحث بالاستيراد | ❌ **4 ملفات ميتة** |

---

## 1) المشاكل المؤكدة (اكتُشفت فعليًا — مش احتمالات)

### 🔴 P0 — يجب إصلاحها قبل أي عرض

| # | المشكلة | الملف | التأثير |
|---|---|---|---|
| 1 | **الهيدر غير مرئي على `/privacy` و `/terms`** — الهيدر `bg-transparent` قبل التمرير، اللوجو `onDark` (أبيض) والروابط `text-white/90`، وصفحة `LegalPage` خلفيتها `bg-white`. الشعار والقائمة يختفون تمامًا. | `layout/Header.tsx:33-48` + `sections/LegalPage.tsx:25` | العميل يفتح "سياسة الخصوصية" فيلاقي هيدر فاضي |
| 2 | **تسريب ذاكرة في Lenis** — `gsap.ticker.add((time)=>{...})` يضيف دالة مجهولة، بينما التنظيف `gsap.ticker.remove(lenis.raf)` يحذف دالة مختلفة. الـ callback يفضل شغال ويستدعي `lenis.raf` على نسخة مدمَّرة. | `hooks/useLenis.ts:17-25` | تراكم tickers مع كل تنقل → بطء تدريجي |
| 3 | **كل الروابط الداخلية (`#anchor`) تختفي تحت الهيدر** — 23 قسم عنده `id` والفوتر يربط لـ `/#news`, `/#gallery`, `/business#energy`، لكن مفيش `scroll-mt` ولا مرة في المشروع، والتوكن `--header-h: 88px` **معرَّف ومستخدم صفر مرة**. | `globals.css:27` + كل الأقسام | الضغط على أي رابط في الفوتر ينزل غلط بـ 88px |
| 4 | **ESLint error: قراءة ref أثناء الرندر** | `layout/PageTransition.tsx:25` | سلوك غير محدد مع React 19 / compiler |
| 5 | **ESLint error: setState متزامن داخل effect** | `sections/SectorDetails.tsx:116` | renders متتالية عند فتح رابط بـ hash |
| 6 | **الصور الخام ~7 ميجا** — 12 ملف > 300KB (`infrastructure.jpg` 1.0M، `energy.jpg` 997K، `hero_bg.jpg` 932K). الهيرو يحمّل 4 صور كلها بـ `loading="eager"`. | `public/images/` + `sections/Hero.tsx` | LCP كارثي على 4G |

### 🟠 P1 — نظام التصميم (المسافات والتصميم)

| # | المشكلة | الدليل |
|---|---|---|
| 7 | **padding الأقسام غير موحّد** — `py-24` (25 مرة)، `py-28` (4 مرات)، `py-20` (مرة). مفيش قاعدة. | `CareersCta` و `ServicesShowcase` بـ py-28، والباقي py-24 |
| 8 | **نصف القطر يخالف قاعدة المشروع نفسه** — تعليق `globals.css:20-24` يقول حرفيًا "قيمتان فقط: md=12px و lg=20px، ممنوع ثالث". الواقع: `rounded-xl` ×35، `rounded-2xl` ×12، `rounded-[2.5rem]` ×3، `rounded-[2rem]` ×2، `rounded-[32px]`، `rounded-md`. يعني **6 قيم** بدل 2. | grep على `rounded-` |
| 9 | **52 لون مكتوب يدويًا خارج التوكنز** — `rgba(15,21,95,0.10)` وحدها متكررة 3 مرات، `#000` ×6، `#0F155F` ×3، `#070a2e` ×2 (لون مش موجود في النظام أصلًا). | 20 ملف |
| 10 | **عرض الحاويات غير موحّد** — `max-w-[1400px]` (30 مرة) هو المعيار، لكن فيه `[1600px]`, `[1100px]`, `[950px]`, `[900px]`, `[820px]` بلا سبب موثّق. | `Presence`, `Hero`, `CtaBanner`, `LegalPage` |
| 11 | **أوزان الخط فوضى** — نفس مستوى العنوان (h2) مرة `font-extrabold` (32 مرة) ومرة `font-black` (39 مرة) ومرة `font-bold`. | `CtaBanner` extrabold مقابل `LegalPage h2` black |
| 12 | **تكرار: نمط "eyebrow + عنوان" مكرر 23 مرة** بنفس الكلاسات المنسوخة (`text-xs font-semibold uppercase tracking-widest`) بدون مكوّن مشترك. | 26 ملف |
| 13 | **3 أزرار Primary مختلفة الارتفاع** — `MagneticButton` (`px-9 py-4`)، زر الفورم (`px-9 py-3.5`)، زر "قدّم الآن" (`px-6 py-2.5`). كلها "الزر الأساسي" بصريًا لكن بأحجام مختلفة. | `MagneticButton.tsx:46`, `ContactForm.tsx:90`, `OpenPositions.tsx:47` |
| 14 | **`MagneticButton variant="outline"` يعمل على الخلفيات الداكنة فقط** (`border-white/40 text-white`) — لا توجد نسخة للخلفيات الفاتحة. | `MagneticButton.tsx:50` |

### 🟠 P1 — الشاشات (التوافق)

| # | المشكلة | الدليل |
|---|---|---|
| 15 | **التابلت مهمَل تقريبًا** — `lg:` مستخدم 160 مرة، `md:` 22 مرة فقط. يعني من 768px لـ 1023px (آيباد بالطول، آيباد Air) الموقع بيعرض **تخطيط الموبايل** بعرض 1000px → أعمدة واحدة ممطوطة ومسافات فاضية. | إحصاء breakpoints |
| 16 | **الهيرو `h-screen`** بدل `dvh/svh` — على iOS Safari شريط العنوان يقص أسفل الهيرو. | `Hero.tsx:54` |
| 17 | **بطاقة المعرض عرضها ثابت `w-[360px]`** داخل حاوية `h-[540px]` — على شاشة 360px تخرج عن الحدود. | `Gallery.tsx:211` |
| 18 | **`grid-cols-2` بدون prefix** في قسمين — عمودان مضغوطان على 360px. | `Sustainability.tsx:38`, `Presence.tsx:94` |
| 19 | **مبدّل اللغة يظهر مرتين** بين `sm` و `lg` — مرة في الهيدر (`sm:block`) ومرة داخل قائمة الموبايل. | `Header.tsx:58,101` |

### 🟠 P1 — الاتجاه واللغة (RTL / i18n)

| # | المشكلة | الدليل |
|---|---|---|
| 20 | **الشريط المتحرك (Ticker) مكسور في العربي** — `flex w-max` + `translateX(-50%)`. في RTL ترتيب العناصر ينعكس فيتحرك في الاتجاه الخطأ ويظهر فراغ. `OurClients` عالج نفس المشكلة بـ `dir` قسري — الـ Ticker لأ. | `Ticker.tsx:10` مقابل `OurClients.tsx:3` |
| 21 | **24 كلاس اتجاه فيزيائي** (`left-`, `right-`, `pl-`, `pr-`, `ml-`, `mr-`) مقابل 28 منطقي — خليط. أخطرها `Values.tsx:176-179` (خط زمني كامل بـ `pl`/`pr`) و`MissionVision.tsx:106`. | grep اتجاهي |
| 22 | **سهم "←" مكتوب كنص** لا ينعكس + **إيموجي 📍** خارج لغة الأيقونات (باقي الموقع SVG مرسوم يدويًا). | `OpenPositions.tsx:41,49` |
| 23 | **`Counter` يستخدم `toLocaleString()` بلا locale** — يتبع لغة المتصفح لا لغة الصفحة → أرقام بصيغة مختلفة لكل زائر. | `Counter.tsx:51` |
| 24 | **الفوتر فيه نص إنجليزي ثابت داخل مكوّن مترجم** — `"Riyadh, KSA"`, `"info@wjeen.com"`, `"+966 11 000 0000"` و`© 2026` كلها hardcoded. | `Footer.tsx:96-98,180` |
| 25 | **روابط سوشيال وهمية** — `https://instagram.com` بدون حساب. | `Footer.tsx:55-58` |

### 🟠 P1 — إتاحة الوصول (a11y)

| # | المشكلة | الدليل |
|---|---|---|
| 26 | **صفر `focus-visible` في الموقع كله** — 4 مطابقات فقط وكلها داخل `ContactForm`. كل الروابط والأزرار والقائمة بلا مؤشر تركيز. | grep |
| 27 | **الفورم: `<label>` بلا `htmlFor`/`id`** في الحقول الخمسة، ولا `aria-invalid`، ولا `aria-describedby`، ورسالة النجاح بلا `role="status"`. | `ContactForm.tsx:49-84` |
| 28 | **قائمة الموبايل**: بلا `aria-expanded`/`aria-controls`، بلا قفل تمرير الخلفية (Lenis يفضل يمرّر)، بلا إغلاق بـ `Esc`، بلا حصر تركيز. | `Header.tsx:59-70` |
| 29 | **لا يوجد `aria-current` على الرابط النشط** — الزائر مش عارف هو في أنهي صفحة. | `Header.tsx:46` |
| 30 | **لا يوجد Skip link** ولا `sr-only` ولا مرة في المشروع. | grep |
| 31 | **المعرض يخطف أسهم لوحة المفاتيح على مستوى `window`** — الأسهم لا تمرّر الصفحة طالما المعرض في الـ DOM. | `Gallery.tsx:105-113` |

### 🟡 P2 — كود ميت وتكرار

| # | البند | الحجم |
|---|---|---|
| 32 | **4 مكوّنات غير مستوردة إطلاقًا**: `sections/Projects.tsx` (78 سطر)، `sections/BusinessSectors.tsx` (102)، `layout/CustomCursor.tsx` (65)، `layout/PageLoader.tsx` (42) | **287 سطر ميت** |
| 33 | **CSS ميت تابع له** — قواعد `.cursor-dot` / `.cursor-ring` / `body.cursor-hover` (37 سطر) لمكوّن محذوف من الشجرة. | `globals.css:68-104` |
| 34 | **توكن ميت** — `--header-h` معرَّف ومستخدم صفر مرة. | `globals.css:27` |
| 35 | **`Projects.tsx` نسخة مكررة من `ProjectsGrid.tsx`** — نفس المحتوى بتخطيط مختلف. | تكرار منطق |
| 36 | **`map.md` ملف فارغ (0 بايت)** غير متتبع. | |
| 37 | **`legacy-static/` و `القديم/` = 7.6 ميجا داخل المستودع** (7 ملفات متتبعة في git). | يبطّئ الـ clone |
| 38 | **`TODO-BACKEND.md` قديم ومضلل** — يقول إن `sitemap.xml` و`robots.txt` وصورة OG وصفحة 404 "مش موجودة"، وكلها **موجودة ومبنية** فعلًا (`app/sitemap.ts`, `app/robots.ts`, `opengraph-image.tsx`, `not-found.tsx`). | |

### 🟡 P2 — جاهزية الإطلاق

| # | البند |
|---|---|
| 39 | `POST /api/contact` يعمل `console.log` فقط — مفيش إرسال إيميل. |
| 40 | لا rate-limit ولا honeypot ولا CAPTCHA على الفورم → سبام مفتوح. |
| 41 | `next.config.ts` بلا headers أمنية (CSP/HSTS/X-Frame-Options) وبلا `images.qualities`. |
| 42 | لا Analytics ولا Cookie consent. |
| 43 | `SITE_URL` لسه placeholder `https://www.wjeen.com`. |

---

## 2) خطة الاختبار — 8 جولات

### الجولة 0 — البوابات الآلية (تُشغَّل قبل كل جولة)
```bash
npm run build            # لازم يمر
npx tsc --noEmit         # صفر أخطاء
npx eslint .             # صفر errors وصفر warnings
node scripts/i18n-parity.mjs   # تطابق مفاتيح en/ar
npx knip                 # ملفات/تصديرات/تبعيات غير مستخدمة
```
**معيار القبول**: الخمسة كلهم أخضر. أضِف `"qa": "npm run build && tsc --noEmit && eslint . && node scripts/i18n-parity.mjs"` في `package.json`.

### الجولة 1 — مصفوفة الصفحات × اللغات (20 حالة)
10 مسارات × لغتين. لكل واحدة: افتح، مرّر من فوق لتحت، سجّل أي شيء غريب.

| المسار | en | ar |
|---|---|---|
| `/` | ☐ | ☐ |
| `/about` | ☐ | ☐ |
| `/business` | ☐ | ☐ |
| `/projects` | ☐ | ☐ |
| `/careers` | ☐ | ☐ |
| `/contact` | ☐ | ☐ |
| `/privacy` | ☐ | ☐ |
| `/terms` | ☐ | ☐ |
| `/not-a-page` (404) | ☐ | ☐ |
| تبديل اللغة من كل صفحة (يحافظ على المسار؟) | ☐ | ☐ |

**معيار القبول لكل صفحة**: (أ) الهيدر مقروء فوق أول قسم، (ب) مفيش تمرير أفقي، (ج) كل نص ظاهر ومترجم، (د) كل الصور محمّلة، (هـ) الفوتر مكتمل.

### الجولة 2 — مصفوفة الشاشات (9 عروض × الصفحات الست الأساسية)
`360` (أصغر أندرويد) · `390` (iPhone) · `430` (iPhone Max) · `768` (iPad portrait) · `834` (iPad Air) · `1024` (iPad landscape) · `1280` (لابتوب) · `1440` (شائع) · `1920` (ديسكتوب)

**ركّز على النطاق 768–1023** — هو المنطقة المكسورة حاليًا (بند 15).
لكل عرض تحقق من:
- ☐ صفر تمرير أفقي: `document.documentElement.scrollWidth <= window.innerWidth`
- ☐ صفر نص مقصوص أو متداخل
- ☐ مساحات لمس ≥ 44×44 px للأزرار
- ☐ الشبكات تنكسر بشكل منطقي (مش عمود واحد بعرض 1000px)
- ☐ الهيرو والـ PageHeader بارتفاع معقول (مش نص شاشة فاضي)
- ☐ Landscape على الموبايل (390×844 مقلوبة)

### الجولة 3 — تطابق RTL (side-by-side)
افتح `/en/x` و `/ar/x` جنب بعض بنفس العرض وقارن **بصريًا كصورة مرآة**:
- ☐ اتجاه كل شبكة وترتيب الأعمدة منعكس
- ☐ كل سهم/أيقونة اتجاهية منعكسة (لا نصوص `→` ثابتة)
- ☐ الشرائط المتحركة (Ticker, OurClients) تتحرك بلا فراغ ولا قفزة
- ☐ الخط الزمني في `Values` منعكس بالكامل
- ☐ الأرقام والتواريخ بصيغة متسقة
- ☐ `Gallery` و `ServicesShowcase` و `MissionVision` (فيهم منطق `rtl` يدوي)
- ☐ مفيش نص إنجليزي ثابت في النسخة العربية (ولا العكس)

### الجولة 4 — تدقيق نظام التصميم (Tokens)
اعمل صفحة داخلية `/dev/tokens` (أو ورقة Figma) تعرض كل الأساسيات، وطابقها مع الكود:
- ☐ **الألوان**: صفر hex/rgba في `.tsx` — كل شيء `var(--color-*)` أو كلاس Tailwind
- ☐ **نصف القطر**: قيمتان فقط (md/lg) زي ما التعليق يقول — أو حدّث التعليق ووثّق 3 قيم بحد أقصى
- ☐ **المسافات**: سلّم واحد لـ padding الأقسام (اقترح: `py-20 md:py-24 lg:py-28`) مطبّق على **كل** قسم
- ☐ **الحاويات**: `max-w-[1400px] px-6 lg:px-10` هو الافتراضي؛ أي استثناء موثّق بتعليق
- ☐ **الطباعة**: سلّم معرّف (h1/h2/h3/body/small) بوزن واحد لكل مستوى
- ☐ **الظلال**: عدد محدود من قيم `shadow-[...]` المخصصة
- ☐ **الأزرار**: مكوّن `Button` واحد بـ variants (`solid` / `outline-light` / `outline-dark` / `ghost`) وحجمين

### الجولة 5 — التفاعل والحركة
- ☐ كل زر/رابط: hover + **focus بلوحة المفاتيح** (Tab) + active
- ☐ التنقل بـ Tab عبر صفحة كاملة بدون ماوس — الترتيب منطقي والتركيز مرئي دائمًا
- ☐ `Esc` يقفل قائمة الموبايل؛ التمرير الخلفي مقفول وقت فتحها
- ☐ **`prefers-reduced-motion`**: فعّلها في DevTools → كل الحركات تتوقف (تحقق خصوصًا من Lenis والـ Ticker وGSAP SplitText — دول مش محميين حاليًا)
- ☐ **بدون JavaScript**: عطّل JS → النصوص المتحركة (`SplitReveal`) لازم تفضل ظاهرة
- ☐ **شبكة بطيئة** (Slow 4G في DevTools): الهيرو مايبقاش أسود لثواني
- ☐ التنقل بين الصفحات ذهابًا وإيابًا 10 مرات ثم افحص الأداء (بند 2 — تسريب Lenis)
- ☐ كل رابط `#anchor` في الفوتر ينزل على العنوان الصحيح مش تحت الهيدر

### الجولة 6 — الوصولية والأداء
```bash
npx @axe-core/cli http://localhost:3000/en http://localhost:3000/ar
npx lighthouse http://localhost:3000/en --preset=desktop --view
npx lighthouse http://localhost:3000/ar --form-factor=mobile --view
```
**أهداف**: Accessibility ≥ 95 · Performance ≥ 85 (موبايل) · Best Practices ≥ 95 · SEO = 100
- ☐ تباين الألوان AA على كل نص (خصوصًا `text-white/70` فوق الصور)
- ☐ تسلسل العناوين سليم: `h1` واحد لكل صفحة ثم h2 ثم h3 بلا قفزات
- ☐ كل صورة زخرفية `alt=""` وكل صورة معلوماتية لها وصف حقيقي
- ☐ قارئ شاشة (VoiceOver على macOS): تصفّح الصفحة الرئيسية بالكامل

### الجولة 7 — SEO والمشاركة
- ☐ `/sitemap.xml` و `/robots.txt` يفتحوا صح ويحتووا المسارات الجديدة (`/privacy`, `/terms`)
- ☐ `hreflang` بين en/ar صحيح في كل صفحة
- ☐ صورة OG تظهر صح: جرّب على [opengraph.xyz](https://www.opengraph.xyz)
- ☐ JSON-LD صالح: [Rich Results Test](https://search.google.com/test/rich-results)
- ☐ عنوان ووصف فريدين لكل صفحة (حاليًا الـ `generateMetadata` في الـ layout بس — الصفحات الداخلية بتورث نفس العنوان)

---

## 3) خطة التنفيذ — 6 مراحل

### المرحلة 0 — تنظيف وإصلاح عاجل *(~نصف يوم)*
1. احذف الأربع مكوّنات الميتة + قواعد الـ cursor في `globals.css` + `map.md`
2. انقل `legacy-static/` و `القديم/` خارج المستودع (`git rm -r --cached` + `.gitignore`)
3. أصلح P0 من 1 لـ 5 (الهيدر على الصفحات الفاتحة، تسريب Lenis، `scroll-mt` من `--header-h`، خطأي ESLint)
4. حدّث `TODO-BACKEND.md` (البنود المنجزة)
✅ **بوابة**: `npm run qa` أخضر بالكامل

### المرحلة 1 — توحيد نظام التصميم *(~يوم)*
1. وسّع `globals.css` بتوكنز مسافات وطباعة وظلال
2. أنشئ `components/ui/Section.tsx` (الحاوية + padding الموحّد) و `SectionHeading.tsx` (الـ eyebrow + العنوان) و `Button.tsx`
3. مرّر على الـ 26 ملف واستبدل النمط المكرر بالمكوّنات
4. استبدل الـ 52 لون اليدوي بتوكنز
5. وحّد نصف القطر والأوزان
✅ **بوابة**: `grep -rE '#[0-9a-fA-F]{6}' src --include='*.tsx'` = صفر (عدا `opengraph-image.tsx`)

### المرحلة 2 — التابلت والاستجابة *(~يوم)*
1. أضف طبقة `md:` لكل شبكة (بند 15) — هدف: نسبة `md:`/`lg:` تقارب 1:1.5
2. `h-screen` → `h-[100svh]`، وبطاقة المعرض تصبح مرنة
3. `grid-cols-2` → `grid-cols-1 sm:grid-cols-2`
4. أصلح ازدواج مبدّل اللغة
✅ **بوابة**: الجولة 2 كاملة خضراء على الـ 9 عروض

### المرحلة 3 — RTL واللغة *(~نصف يوم)*
1. حوّل الـ 24 كلاس فيزيائي لمنطقي (`ps-`/`pe-`/`start-`/`end-`)
2. أصلح الـ Ticker في RTL
3. انقل نصوص الفوتر الثابتة لملفات الترجمة + سنة ديناميكية
4. `Counter` يستخدم `useLocale()`
5. استبدل `→` و`📍` بأيقونات SVG
✅ **بوابة**: الجولة 3 كاملة خضراء

### المرحلة 4 — الوصولية *(~نصف يوم)*
1. حلقة `focus-visible` عامة في `globals.css`
2. أصلح الفورم بالكامل (labels/aria/role)
3. أصلح قائمة الموبايل (aria/Esc/قفل التمرير)
4. `aria-current` + Skip link
5. اربط اختصارات المعرض بالحاوية بدل `window`
✅ **بوابة**: axe = صفر انتهاكات، Lighthouse a11y ≥ 95

### المرحلة 5 — الأداء والإطلاق *(~نصف يوم)*
1. اضغط الـ 12 صورة (هدف: ≤ 200KB للصورة) — `sharp` أو Squoosh
2. `priority` للصورة الأولى فقط في الهيرو، والباقي كسول
3. headers أمنية في `next.config.ts`
4. اربط `/api/contact` بمزود إيميل + honeypot + rate-limit
5. روابط سوشيال حقيقية + `SITE_URL` النهائي
✅ **بوابة**: Lighthouse Performance ≥ 85 موبايل

---

## 4) أدوات مقترحة للأتمتة (اختياري لكن يوفّر وقت كتير)

| الأداة | تكشف |
|---|---|
| `knip` | ملفات/تصديرات/تبعيات ميتة تلقائيًا |
| `playwright` + `toHaveScreenshot` | لقطات مرجعية لـ 10 صفحات × 2 لغة × 4 عروض = 80 لقطة، وأي تغيير تصميمي مستقبلي يبان فورًا |
| `@axe-core/playwright` | فحص a11y داخل نفس الاختبارات |
| `eslint-plugin-tailwindcss` | يمنع القيم العشوائية `[...]` خارج النظام |
| `lighthouse-ci` | بوابة أداء في الـ CI |

الحد الأدنى المفيد: **knip + playwright screenshots**. الاتنين بيمنعوا رجوع نفس المشاكل تاني بعد ما نصلّحها.
