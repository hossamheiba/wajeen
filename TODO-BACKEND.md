# وجين — الناقص (Backend / Infra / محتوى)

> **الحالة**: الفرونت إند مكتمل بمحتوى **حقيقي** مستخرج من ملف الشركة
> (`PROFILE - Update 04.10.2021.pdf`) — 6 صفحات (Home / About / Business /
> Projects / Contact / Careers) بعربي وإنجليزي. المحتوى ثابت في ملفات الترجمة.
> الملف ده قائمة بكل حاجة لسه ناقصة.

---

## 🔴 لازم قبل الإطلاق

### 1. إرسال إيميل حقيقي
- `POST /api/contact` بيستدعي `sendContactEmail()` من `src/lib/mailer.ts` (Resend)
  ومعاه rate limit (`src/lib/rateLimit.ts`) و honeypot — **بس مفيش API key**،
  فالإرسال مش شغال فعليًا.
- **المطلوب**: `MAIL_PROVIDER_API_KEY` في `.env.local` (شوف `.env.example`)،
  وتحديد الإيميل اللي هتوصله الرسائل.
- زرار "Send Your CV" في Careers بيروح على `corporate@wjeen.com` (عنوان حقيقي من الملف).

### 2. الدومين والاستضافة
- `NEXT_PUBLIC_SITE_URL` قيمته الافتراضية `https://www.wjeen.com` — منها بتتبني
  الـcanonical والـhreflang و`sitemap.xml` و`robots.txt` و JSON-LD.
  `.env.local` المحلي بيوجّهها لـ localhost وهو مستبعد من git.
- محتاج: استضافة (Vercel أنسب مع Next.js) + الدومين + SSL.

### 3. مراجعة قانونية
- **صفحتا سياسة الخصوصية وشروط الاستخدام اتشالوا** بناءً على طلب العميل
  (الملف مفيهوش أي نص قانوني، والنص اللي كان موجود مكتوب من الصفر).
  لو الموقع هيجمع بيانات عبر نموذج التواصل، غالبًا هيحتاجهم قبل الإطلاق.

### 4. تصوير احترافي
- أربع صور full-bleed عامة (`hero_bg` / `buildings` / `energy` /
  `infrastructure`) في الـhero وترويسات الصفحات — **مش شغل وجين**
  (برج بالرياض، محطة طاقة شمسية، تقاطع طرق). **العميل قرر إبقاءها مؤقتًا.**
- صور الملف الحقيقية دقتها صغيرة (471×317) ومش صالحة full-bleed.

---

## 🟡 مهم مش عاجل

- **محتوى ديناميكي / CMS** — 32 مشروع و22 عميل و11 خدمة و7 شهادات كلهم في
  `src/messages/{en,ar}.json`. أي تحديث محتاج deploy. القرار المطلوب: CMS جاهز
  (Sanity / Contentful) ولا داش بورد مخصص؟
- **صفحة تفاصيل مشروع** `/projects/[slug]` — بقت أهم مع 32 مشروع.
- **صور المشاريع**: 10 بس من الـ32 عندها صورة موثّقة من الملف؛ الباقي بيترسم
  ببانل يعرض العمالة والمعدات. أي صور إضافية لازم تيجي **مربوطة باسم مشروعها**
  عشان ما يتكررش خطأ ربط صورة بمشروع غلط.
- **لا أخبار ولا وظائف شاغرة** في الملف — قسم الأخبار اتشال، وصفحة الوظائف
  بتعرض حالة فاضية أمينة. محتاجين داتا حقيقية من العميل لو هيتفعّلوا.
- **Lightbox للمعرض** · **ضغط الصور الأصلية** (4.6MB، منهم 3.8 للأربع صور العامة)
- **Analytics** + **Cookie consent** (لو هيتضاف تتبع)

## ⚪ اختياري

- axe + Lighthouse — لسه ما اتقاسوش ولا مرة
- تحسين التابلت (`md:` مستخدمة 13 مرة مقابل `lg:` 112)
- اختبارات آلية · CI/CD · بحث داخل الموقع · لغات إضافية

---

## ملاحظات تقنية

- Next.js 16 (App Router) + TypeScript + Tailwind v4 + next-intl (`/en`, `/ar`).
- نظام التصميم: `Button.tsx` (3 أنماط × 3 مقاسات)، `Chip.tsx` (عائلتان:
  `Chip` مستطيلة للتسميات و`StatusPill` دائرية للحالات)، `SectionHeading.tsx`،
  سلّم خطوط `t-display`…`t-eyebrow` (**وزن واحد لكل فئة — لا تضف `font-*` فوقها**)،
  ونصف أقطار `rounded-ui` / `rounded-frame`. كله في `src/app/globals.css`.
- `ContactForm` بيستخدم `react-hook-form` + `zod` — الـvalidation جاهز.
- **تحذير**: تغيير ملف صورة ورا نفس الـURL مش بيغيّر اللي المستخدم يشوفه —
  Next بيـcache الصور المحسّنة حسب (url, width, quality) في `.next/cache/images`.
  ادّي الملف الجديد اسم جديد.
- هوية الألوان: كحلي `#0F155F` + أبيض + رمادي.
