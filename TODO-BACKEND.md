# وجين وشركاه — الناقص (Backend / Dashboard / Infra)

> **الحالة الحالية**: التصميم الفرونت إند شغال بالكامل (6 صفحات: Home / About / Business / Projects / Contact / Careers) بمحتوى ثابت (hardcoded) في ملفات الترجمة. الملف ده قائمة بكل حاجة اتأجلت عمدًا لحد ما نبدأ شغل الباك إند والداش بورد.

---

## 🔴 أساسي (لازم قبل الإطلاق الفعلي)

### 1. إرسال إيميل حقيقي
- **الوضع الحالي**: `POST /api/contact` (في `src/app/api/contact/route.ts`) بيعمل validate للبيانات وبس بيطبعها في server logs. مفيش أي إيميل بيتبعت فعليًا.
- **زرار "Send Your CV"** في صفحة Careers بيعمل `mailto:careers@wjeen.com` — إيميل وهمي، محتاج الإيميل الحقيقي.
- **المطلوب**: توصيل خدمة إيميل (Resend / SendGrid / أي حاجة تانية) + تحديد الإيميل الحقيقي اللي هتوصله الرسائل.

### 2. محتوى ديناميكي / CMS
- **الوضع الحالي**: كل المحتوى (9 مشاريع، فريق القيادة، 6 وظائف، الأخبار، إلخ) موجود hardcoded جوه:
  - `src/messages/en.json` و `src/messages/ar.json`
  - `src/lib/saudiMap.ts` (بيانات الخريطة والمناطق)
- **المشكلة**: أي تحديث (مشروع جديد، وظيفة جديدة، خبر جديد) محتاج تعديل كود ونشر (deploy) من جديد.
- **المطلوب**: قرار — CMS جاهز (Sanity / Contentful / Strapi) ولا داش بورد مخصص + قاعدة بيانات؟

### 3. بيانات حقيقية بدل الـ Placeholder
- **فريق القيادة** (صفحة About) — الأسماء والمناصب والصور كلها placeholder دلوقتي (نص "بيانات القيادة الحقيقية والصور في انتظار العميل" ظاهر في الصفحة).
- **المشاريع التسعة** — عناوين ومواقع وقيم واقعية بس مش مشاريع حقيقية موثقة.
- **الوظائف الستة** — أمثلة واقعية بس مش وظائف شاغرة فعليًا.
- **بيانات التواصل** (العنوان، الهاتف، الإيميل) — placeholder.

### 4. SEO أساسي ✅ (تم)
- `sitemap.xml` — ✅ موجود (`src/app/sitemap.ts`)
- `robots.txt` — ✅ موجود (`src/app/robots.ts`)
- صورة Open Graph مخصصة — ✅ موجودة لكل لغة (`src/app/[locale]/opengraph-image.tsx`)
- Structured data (JSON-LD للشركة) — ✅ موجود (`src/app/[locale]/layout.tsx`)
- لسه ناقص: JSON-LD للمشاريع الفردية (محتاج صفحات تفاصيل مشروع أولًا، بند تحت)

### 5. الاستضافة والدومين
- الموقع لسه شغال محليًا على `localhost` بس.
- محتاج: استضافة (Vercel أسهل اختيار مع Next.js) + دومين حقيقي + شهادة SSL.

---

## 🟡 مهم (يستاهل يتعمل، مش urgent)

- **صفحة مشروع مستقلة** `/projects/[slug]` — دلوقتي كل مشروع card بس، مفيش صفحة تفاصيل.
- **صفحة خبر مستقلة** `/media/[slug]` — "المركز الإعلامي" بيوصل لقسم في نفس الصفحة بس.
- **Lightbox لمعرض الصور** — فتح الصور بالحجم الكامل.
- **ضغط الصور الأصلية** — الصور خام (~1MB للواحدة)، Next.js بيحسّنها وقت الطلب بس الأصل ينفع يتضغط من الأساس.
- **صفحة 404 مخصصة** ✅ (تم) — `src/app/[locale]/not-found.tsx` + catch-all `[locale]/[...rest]`.
- **Analytics** (Google Analytics / Plausible / إلخ) — مفيش تتبع زوار خالص.
- **Cookie consent banner** — لو هيتضاف أي تتبع، غالبًا مطلوب قانونيًا في السعودية/عمومًا.

## ⚪ اختياري / مستقبلي

- نظام تسجيل دخول لو هيبقى فيه داش بورد إدارة محتوى
- Unit tests / e2e tests — مفيش خالص دلوقتي
- CI/CD pipeline (GitHub Actions أو مشابه) للنشر التلقائي
- Multi-language لغات تانية غير عربي/إنجليزي (لو مطلوب مستقبلًا)
- نظام بحث في الموقع

---

## ملاحظات تقنية للرجوع ليها وقت شغل الباك إند

- المشروع Next.js 16 (App Router) + TypeScript + next-intl (عربي/إنجليزي، `/ar` و `/en`).
- الفورم الحالي (`src/components/sections/ContactForm.tsx`) بيستخدم `react-hook-form` + `zod` — الـ validation جاهز، محتاج بس نوصله بخدمة إرسال حقيقية بدل الـ log.
- `MagneticButton` (`src/components/ui/MagneticButton.tsx`) بيفرّق تلقائيًا بين اللينكات الداخلية (locale-aware) والخارجية (`mailto:`, `tel:`, `http`) — أي رابط خارجي جديد هيشتغل صح من غير تعديل.
- هوية الألوان: كحلي `#0F155F` (أصلي من اللوجو) + أبيض + رمادي — موثقة في `src/app/globals.css`.
